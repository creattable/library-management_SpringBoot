<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qin.web.book_borrow.mapper.BorrowBookMapper">
    
    <select id="getBorrowList" resultType="com.qin.web.book_borrow.entity.ReturnBook">
        select b.borrow_id,b.book_id,sb.book_name,sb.book_place_num, sr.username,sr.learn_num,sr.class_name,sr.phone,b.borrow_time,b.return_time,b.borrow_status
        from  borrow_book as b left join sys_books as sb on b.book_id = sb.book_id
        left join sys_reader as sr  on b.reader_id = sr.reader_id
        where 1=1
        <if test="parm.username != null and parm.username !=''">
            and sr.username =#{parm.username}
        </if>
        <if test="parm.borrowStatus != null and parm.borrowStatus !=''">
            and b.borrow_status =#{parm.borrowStatus}
        </if>
        order by b.borrow_time desc
    </select>
</mapper>